let package="express";
switch(package){
     case "standard": console.log(`might take 3-5 days`);
     break;
     case "express": console.log(`might take 1-2 days`);
     break;
     case "overnight": console.log(`would be deliverred the next day`);
     break;
     default: console.log(`Enter the valid pakage is`);
}
